<lead_feedback ticket="{{TICKET_ID}}" turn="{{TURN}}">
{{FEEDBACK}}
</lead_feedback>
{{VERIFICATION_BLOCK}}
Continue the same work package with this feedback. The ownership, acceptance, environment, operating rules, and blocker protocol from the original package still apply. Fix the underlying cause rather than the symptom, re-run the relevant checks, and return the final report JSON again describing the whole package as it stands now, not only this delta.
